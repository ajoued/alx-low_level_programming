#include "main.h"
#include <stdio.h>

/**
 * main - Entry point
 * @argc: arguments count
 * @argv: array of arrguments
 * Return: 0 Always success
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", argv[0]);
	return (0);
}
