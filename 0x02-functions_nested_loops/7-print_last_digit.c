#include "main.h"
/**
 * print_last_digit - prints the last digit of a number
 *
 * @n: the digit to be treated
 *
 * Return: the value of the last digit
 */
int print_last_digit(int n)
{
	int a;

	a = n % 10;
return (_abs(a));
}
