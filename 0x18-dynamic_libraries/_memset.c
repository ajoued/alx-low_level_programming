#include "main.h"

/**
 * _memset - random description
 * @s: random description
 * @b: random description
 * @n: random description
 * Return: random return
*/
char *_memset(char *s, char b, unsigned int n)
{
	return (NULL);
}
